import React from 'react';

const Heading = () => {
  return (
    <h2>
      Select the date <span style={{ color: 'violet' }}>FROM</span> and
      <span style={{ color: 'violet', marginBottom: '50px' }}> TO</span> to see
      the number of diseases on Covid
    </h2>
  );
};

export default Heading;
